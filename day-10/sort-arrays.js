//Solo un uso de clasificación simple. Cree una función que devuelva los elementos de la lista/matriz de entrada
//ordenados en orden lexicográfico.


const arraySort = (arr) =>{
    return arr.sort()
}


console.log(arraySort(['one', 'two', 'three' ]))