const uppercase = (string) =>{
    if(string === string.toUpperCase()){
        return true
    }else{
        return false
    }
}

console.log(uppercase("jose"))

// return this == this.toUpperCase()