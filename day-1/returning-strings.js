// Cree una función que devuelva una declaración
//de saludo que use una entrada; su programa debería regresar:
//  "Hello, <name> how are you doing today?".


const greet = (name) => {
    return `Hello, ${name} how are you doing today?`;
  };
  
  console.log(greet("Francisco"));